@page
@model TESTASP_DCPR.Pages.Dgi.Etape1
@{
    ViewData["Title"] = "Etape1";
}

<div class="section-title">
    <h2>ETAPE 1</h2>
</div>

<div class="row justify-content-center">
    <div class="col-md-8 mx-auto">
        <form action="" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
            <label class="control-label">
                Entrez le numero de votre fiche
            </label>
            <input required asp-for="fiche" class="form-control">
            </div><br>
         <div class="form-group">
            <input class="btn btn-primary"  type="submit" value="Go">
            </div>
            @if (Model.Exist==null)
{
    
    
}else{
    foreach(var n in Model.Exist){
        <div class="display-flex-right"></div>
                    ViewData["message"] = "fiche existant tu peux passer a la caisse";
                    <a asp-page="./Create" class="btn btn-primary" asp-route-id="@n.noFiche">preceed to the paiment</a>
    }
}
        </form>

    </div>
    
</div>
<style>



.left-section {
  flex: 1; /* La section de gauche occupera 1/2 de l'espace disponible */
  padding: 20px;
  /* Ajoutez ici les styles spécifiques à la section de gauche (par exemple, pour l'image) */
}
h1, h2, h3, h4, h5, h6 {
        font-family: "Raleway", sans-serif;
        }
        .section-title {
        text-align: center;
        padding-bottom: 30px;
        }
        .section-title h2 {
        font-size: 32px;
        font-weight: bold;
        margin-bottom: 20px;
        padding-bottom: 20px;
        position: relative;
        color: #2c4964;
        }
        .section-title h2::before {
  content: '';
  position: absolute;
  display: block;
  width: 120px;
  height: 1px;
  background: #ddd;
  bottom: 1px;
  left: calc(50% - 60px);
}

.section-title h2::after {
  content: '';
  position: absolute;
  display: block;
  width: 40px;
  height: 3px;
  background: #1977cc;
  bottom: 0;
  left: calc(50% - 20px);
}

.section-title p {
  margin-bottom: 0;
}


    
</style>
