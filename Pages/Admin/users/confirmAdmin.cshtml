@page
@model TESTASP_DCPR.Pages.Admin.users.confirmAdmin
@{
    ViewData["Title"] = "confirmAdmin";
}


<div class="section-title">
    <h2>CONFIRM ADMIN</h2>

</div>
<div class="row justify-content-center">
    <div class="col-md-8">
    <form action="" method="post">
        <div asp-validation-summary="ModelOnly" class="text text-danger"></div>
        <div class="form-group">
            <label class="control-label">Username Admin</label>
            <input required name="username" asp-for="username" class="form-control"  type="text" placeholder="Password">
            <span></span>

        </div>
        <div class="form-group">
            <label class="control-label">Password Admin</label>
            <input name="password" required type="password" asp-for="Password" class="form-control"   placeholder="Password">
            <span></span>

        </div>
        <input type="submit" class="btn btn-primary" value="Verification">
    </form>


</div>
</div>


<style>



.left-section {
  flex: 1; /* La section de gauche occupera 1/2 de l'espace disponible */
  padding: 20px;
  /* Ajoutez ici les styles spécifiques à la section de gauche (par exemple, pour l'image) */
}
h1, h2, h3, h4, h5, h6 {
        font-family: "Raleway", sans-serif;
        }
        .section-title {
        text-align: center;
        padding-bottom: 30px;
        }
        .section-title h2 {
        font-size: 32px;
        font-weight: bold;
        margin-bottom: 20px;
        padding-bottom: 20px;
        position: relative;
        color: #2c4964;
        }
        .section-title h2::before {
  content: '';
  position: absolute;
  display: block;
  width: 120px;
  height: 1px;
  background: #ddd;
  bottom: 1px;
  left: calc(50% - 60px);
}

.section-title h2::after {
  content: '';
  position: absolute;
  display: block;
  width: 40px;
  height: 3px;
  background: #1977cc;
  bottom: 0;
  left: calc(50% - 20px);
}

.section-title p {
  margin-bottom: 0;
}


    
</style>